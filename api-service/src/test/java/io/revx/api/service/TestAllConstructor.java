package io.revx.api.service;

//@RunWith(JMockit.class)
public class TestAllConstructor {
  /*
   * 
   * @Test public void testPrivateConstructorApiConstant() throws Exception {
   * java.lang.reflect.Constructor<ApiConstant> constructor =
   * ApiConstant.class.getDeclaredConstructor(); assertTrue("Constructor is not private",
   * Modifier.isPublic(constructor.getModifiers())); constructor.setAccessible(true);
   * constructor.newInstance(); }
   * 
   * @Test public void testPrivateConstructorPerformanceDataId() throws Exception {
   * java.lang.reflect.Constructor<PerformanceDataId> constructor =
   * PerformanceDataId.class.getDeclaredConstructor(); assertTrue("Constructor is not private",
   * Modifier.isPublic(constructor.getModifiers())); constructor.setAccessible(true);
   * constructor.newInstance(); }
   * 
   * @Test public void testPrivateConstructorPerformanceData() throws Exception {
   * java.lang.reflect.Constructor<PerformanceData> constructor =
   * PerformanceData.class.getDeclaredConstructor(); assertTrue("Constructor is not private",
   * Modifier.isPublic(constructor.getModifiers())); constructor.setAccessible(true);
   * constructor.newInstance(); }
   * 
   * @Test public void testPrivateConstructorPerformanceDataRepositoryImpl() throws Exception {
   * java.lang.reflect.Constructor<PerformanceDataRepositoryImpl> constructor =
   * PerformanceDataRepositoryImpl.class.getDeclaredConstructor();
   * assertTrue("Constructor is not private", Modifier.isPublic(constructor.getModifiers()));
   * constructor.setAccessible(true); constructor.newInstance(); }
   */}
